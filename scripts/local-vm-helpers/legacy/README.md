# Legacy Helpers

These launchers were removed from the main helper folder to keep the active workflow
cleaner and more professional.

They are kept here only as an archive in case you need to revisit older local tasks.

Archived here:
- Cloudflare DDNS helpers
- Cloudflare mTLS helper
- older port-only launchers that overlap with `project_debug_report_kali.bat`

Active launchers now live one level up in `scripts/local-vm-helpers/`.
